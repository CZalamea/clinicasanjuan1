name: clinicasanjuan
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.10.0
  firebase_auth: ^5.4.0
  cloud_firestore: ^5.6.1
  firebase_database: ^11.0.0
  url_launcher: ^6.1.10
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  webfeed: ^0.7.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/cruzazullogo.png
    - assets/sanasanalogo.png
    - assets/fybecalogo.png
